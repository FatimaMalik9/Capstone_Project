version: '3.8'

services:
  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    networks:
      - app_networks

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    networks:
      - app_networks
    depends_on:
      - mongo
    

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "4200:80"
    networks:
      - app_networks
    depends_on:
      - backend
    environment:
      - NODE_ENV=production
      - MONGO_URI=mongodb+srv://fatimaumer107:GWHnkmJEyJxsgT5c@cluster0.sjhyz.mongodb.net/cabBooking?retryWrites=true&w=majority&appName=Cluster0

networks:
  app_networks:
    driver: bridge



